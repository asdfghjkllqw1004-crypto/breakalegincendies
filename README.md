# INCENDIES Performance Profile Site

수원여자대학교 연기영상과 공연 프로필 아카이브 사이트입니다.

## 실행

```bash
npm install
npm run dev
```

## 배포

Vercel에서 Framework Preset은 `Vite`, Build Command는 `npm run build`, Output Directory는 `dist`로 설정하세요.

## 이미지

프로필 사진과 연습 사진은 `public/images` 폴더에 포함되어 있습니다.
